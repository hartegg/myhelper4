[
    {{- with $.Site.GetPage "/posts" }}
      {{- range $index, $data := .Pages }}
      {{- if $index }},{{ end }}
      {
        "title": "{{ $data.Title  }}",
        "summary": "{{ $data.Summary  }}",
        "url": "{{ $data.RelPermalink }}"
      }
      {{- end }}
    {{- end }}
  ]
